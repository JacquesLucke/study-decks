{
  "compilerOptions": {
    "jsx": "preserve",
    "sourceMap": true,

    // Needed so that dynamic imports are working.
    "module": "esnext",
    "target": "ESNext",

    "moduleResolution": "node"
  }
}
